"""Provider/integration adapters for Reins (model backends, later ORM/sandbox).

Each adapter is an optional extra so the core stays dependency-free; import the one
you need (e.g. ``from reins.adapters.anthropic import AnthropicModel``).
"""
